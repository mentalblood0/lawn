env:
  log:
    path: /tmp/lawn/log.dat
  data_storage:
    dir: /tmp/lawn
    max_element_size: 65536
  index:
    path: /tmp/lawn/index.idx
seed: 0
amount: 300_000
size:
  key:
    min: 1
    max: 1024
  value:
    min: 1
    max: 1024
benchmarks:
- env add
- env checkpoint
- env random get
# - data storage add
# - data storage random get
# - aligned list add/delete
# - aligned list random get
