env:
  log:
    io:
      file:
        filename: /tmp/lawn/log.dat
        mode: a+
        perm:
        - OwnerAll
        - GroupRead
      sync: true
  indexes:
    dir: /tmp/lawn
    pointer_size: 5
  data_storage:
    data_dir: /tmp/lawn
    logarithmically_divided_sizes_scale:
      max: 65536
      points: 327
seed: 1

